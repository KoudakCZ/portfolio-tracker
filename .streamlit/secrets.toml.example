# This app currently does not require any secrets.
# If you add credentials later, copy this file to `.streamlit/secrets.toml`
# locally and paste the same keys into Streamlit Community Cloud -> App settings -> Secrets.

# EXAMPLE_API_KEY = "your-api-key"
